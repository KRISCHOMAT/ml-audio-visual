{
  "name": "ml-audio-visual-ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "postinstall": "npm run build",
    "start": "vite --host 0.0.0.0 --port $PORT preview"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.3.2"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^4.4.0"
  },
  "engines": {
    "node": "18.15.0"
  }
}
